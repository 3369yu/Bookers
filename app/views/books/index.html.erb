<h1>Books</h1>

<div>
  <span>Title</span>
  <span>Body</span>
</div>
  <body>
    <% @book.each do |book| %>
    <span><%= book.title %></span>
    <span><%= book.body %></span>
    <span> <%= link_to 'Show', book_path(book.id) %></span>
    <span> <%= link_to 'Edit', edit_book_path(book.id) %></span>
    <span> <%= link_to 'Destory', book_path(book.id), methos: :delete, "data-confirm" =>"Are you sure?" %></span>
  <body>
<% end %>