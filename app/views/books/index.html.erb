<h1>Books</h1>

<div>
  <span>Title</span>
  <span>Body</span>
</div>
  <body>
    <% @book.each do |book| %>
    <span><%= book.title %></span>
    <span><%= book.body %></span>
    <span> <%= link_to 'Show', book_path(book.id) %></span>
    <span> <%= link_to 'Edit', book_path(book.id) %></span>
    <span> <%= link_to 'Destory', book_path(book.id) %></span>
  <body>
<% end %>